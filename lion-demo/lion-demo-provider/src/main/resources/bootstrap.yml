spring:
  application:
    name: lion-demo-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        metadata:
          version: 1.0
      config:
        server-addr: localhost:8848
        file-extension: yml