spring:
  application:
    name: lion-demo-sample
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        metadata:
          version: v1.0
      config:
        server-addr: localhost:8848
        file-extension: yml